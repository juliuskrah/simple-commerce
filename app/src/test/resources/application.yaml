spring:
  sql:
    init:
      mode: never
  jpa:
    defer-datasource-initialization: false
    properties:
      jakarta.persistence.sql-load-script-source: /import.sql
logging:
  level:
    org.hibernate.SQL: info
    org.hibernate.HQL_FUNCTIONS: debug
    com.simplecommerce.product.CategoryController: debug
