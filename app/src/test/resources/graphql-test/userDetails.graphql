query me {
    me {
        id
        username
        email
        createdAt
        updatedAt
        userType
        lastLogin
    }
}

mutation createUser($input: CreateUserInput!) {
    addUser(input: $input) {
        id
        username
        email
    }
}